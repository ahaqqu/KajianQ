{
  "upstream": "https://github.com/ahaqqu/agentic-project-template.git",
  "overwrite": [
    ".github/workflows/e2e.yml",
    ".github/workflows/vuln-scan.yml",
    ".github/workflows/dependabot-auto-merge.yml",
    "scripts/template-sync/",
    "scripts/check-agentic-limits.mjs",
    "scripts/check-template-truth.mjs",
    "scripts/openapi-check.mjs",
    "flake.nix",
    "tsconfig.base.json",
    "tsconfig.json",
    "vitest.config.ts"
  ],
  "merge": [
    ".github/workflows/ci.yml",
    ".github/workflows/staging.yml",
    ".github/workflows/template-sync.yml",
    ".agents/skills/"
  ],
  "_comment_ci": "ci.yml, staging.yml and template-sync.yml moved from overwrite to merge: fork CI/CD workflows wire fork-specific infrastructure (NEON_DATABASE_URL into test; Cloudflare secrets + fork-owned deploy steps that replaced the template's D1 flow; bun install + bun 1.4 for the workspace-package imports in template-sync) into their steps, so they are fork-owned in practice. Decided in #4's PR review; staging.yml re-forked after a template auto-resolution regressed the #3 D1 strip; template-sync.yml re-forked after the #54 Neon commit broke the sync run (missing install, bun 1.3 lockfile mismatch)."
}