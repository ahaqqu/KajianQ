{
  "upstream": "https://github.com/ahaqqu/agentic-project-template.git",
  "overwrite": [
    ".github/workflows/e2e.yml",
    ".github/workflows/vuln-scan.yml",
    ".github/workflows/dependabot-auto-merge.yml",
    ".github/workflows/template-sync.yml",
    "scripts/template-sync/",
    "scripts/check-agentic-limits.mjs",
    "scripts/check-template-truth.mjs",
    "scripts/openapi-check.mjs",
    "flake.nix",
    "tsconfig.base.json",
    "tsconfig.json",
    "vitest.config.ts"
  ],
  "merge": [
    "apps/",
    "packages/",
    "package.json",
    "README.md",
    "CONTEXT.md",
    "bun.lock",
    ".github/workflows/ci.yml",
    ".github/workflows/staging.yml"
  ],
  "_comment_ci": "ci.yml and staging.yml moved from overwrite to merge: fork CI/CD workflows wire fork-specific infrastructure (NEON_DATABASE_URL into test; Cloudflare secrets + fork-owned deploy steps that replaced the template's D1 flow) into their steps, so they are fork-owned in practice. Decided in #4's PR review; staging.yml re-forked after a template auto-resolution regressed the #3 D1 strip."
}