# KajianQ & DARS — Specification, Architecture & Plan

> **Version:** 2.1 (2026-08-28) — living document; supersedes `INITIAL_IDEA.md` (v1.2; frozen, retained for history)
> **Status:** Grilled & approved via grill-with-docs session; decisions recorded in `adr/` (0005 onward, full index in §8), vocabulary in `CONTEXT.md`
> **Doc language:** English (technical) — UI copy is Indonesian-first (en/id externalized)

---

## 1. Vision & Objectives

### 1.1 What this is

- **KajianQ** — an open-source (MIT) Islamic classical-knowledge chatbot for the **general Indonesian Muslim public** (not scholars), chatting in **Indonesian and English** over a corpus of **original Arabic sources**: Quran (Uthmani), hadith (with sanad & grades), and **pre-600 H kitab** (fiqh, aqidah, tasawuf, history).
- **DARS** (_Dynamic Automated RAG Solution_; evokes Arabic _dars_, "lesson") — the generic, domain-agnostic RAG engine underneath: ingestion, Smart Router, retrieval, generation, evaluation. Engine code contains **no Islamic-domain logic**; KajianQ is a domain pack + apps on top.

Both live in **one monorepo** (ADR-0005): DARS as workspace packages under `packages/`, KajianQ under `apps/`. Reusability (public-service chat, internal-company knowledge) is a stated long-term goal, enforced by package boundaries — not by building a platform prematurely.

### 1.2 Long-term mission

Improve the Islamic character (_akhlak_) of Indonesian Muslims — youth in particular — by making it easy to get answers grounded in classical Islamic sources. The v1 product serves the **general public**; the youth mission is served through accessibility (plain Indonesian, mobile-first PWA), not by narrowing the audience.

### 1.3 Success for v1

**Real public users at small scale.** The product is deployed, open-source, and used by real people. Success = users receive trustworthy, strictly-sourced answers, and the system visibly improves through user feedback. This justifies the traceability, feedback, and evaluation infrastructure being in scope for v1.

### 1.4 Differentiators (moat)

| Feature         | Typical market         | KajianQ                                                                   |
| --------------- | ---------------------- | ------------------------------------------------------------------------- |
| Sources         | Quran + Bukhari/Muslim | + pre-600 H kitab (Mudawwanah, Al-Umm, Ihya, Tabari, …)                   |
| Corpus language | Modern translations    | Original Arabic + labeled machine translation                             |
| Chat language   | English/Arabic-first   | **Indonesian-first** + English                                            |
| Citations       | Partial                | Strict: QS Surah:Ayah · HR Book No. (Grade) · Kitab, Author, Vol:Page:Bab |
| Madzhab         | Opaque                 | Filter/compare; Syafi'i-weighted with transparent gaps                    |
| Reasoning       | Naive RAG              | Principle-aware: general maxims retrieved alongside rulings               |
| Transparency    | Black box              | **Full user-facing Trace** of how each answer was built                   |
| Data quality    | Raw import             | LLM-cleaned, LLM-translated, harness-validated                            |

### 1.5 Hard boundaries (never cross)

1. **Never issue personal fatwa.** The system presents what classical sources say, with citations, and always ends with: _"Untuk fatwa spesifik, silakan konsultasi ulama setempat."_
2. **Never answer beyond the retrieved context.** If retrieval is insufficient, say so explicitly.
3. **Never hide the machinery.** Every answer carries its full Trace (ADR-0007).
4. **Never silently machine-translate.** All kitab translations are labeled _"Terjemahan mesin — lihat teks Arab asli"_ with the Arabic always shown (ADR-0006).
5. **Dhaif hadith is always flagged** with a warning.
6. **Never derive a new ruling.** The Generator surfaces classical reasoning (ta'lil, madzhab disagreement, applied Principles) _as cited_; it never synthesizes novel rulings, never resolves disagreements, never upgrades a grade. Insufficient evidence → refusal, not synthesis (ADR-0015).
7. **No corpus-wide inferred knowledge graphs.** Knowledge layers are bounded, curated, human-reviewed concept structures (#24 terminology graph, #29 isnad, Principle Index, curated `concept_links`) — never a community-GraphRAG corpus extraction (ADR-0016).

---

## 2. Product Specification

### 2.1 Capabilities

- **Bilingual chat (ID/EN)** with automatic language detection; answers in the user's language; Arabic originals shown for every quoted ayah/hadith/kitab passage.
- **Strict citations** — Quran: `QS. Al-Baqarah:255`; Hadith: `HR. Bukhari no. 573 (Sahih)`; Kitab: `Al-Umm, Imam Syafi'i, Jilid 1, Hal. 102, Bab …`.
- **Madzhab policy (Syafi'i-weighted, transparent gaps).** Corpus ingests all madhhabs available. When the user does not specify: lead with the Syafi'i view (Indonesian majority), note other madzhab views _if the corpus covers them_, otherwise state _"pendapat madzhab lain belum tersedia dalam korpus"_. Users may set a madzhab preference or explicitly compare madhhabs. Differences are presented neutrally — never attacking any school.
- **Hadith grade transparency.** Grades (`mutawatir | sahih | hasan | dhaif`) are always displayed; dhaif material carries a warning.
- **Principle-aware answers.** Why/analogy questions retrieve **Principles** (yusr, rahmah, masyaqqah, dharar, umum al-balwa, istihsan, sadd al-dhara'i, …) alongside specific rulings, so answers explain the _lens_, not just the rule.
- **Full Trace panel (user-facing).** Every answer expands to show: router intent, sub-queries, retrieved chunks with scores, model identity. General users see a readable "sources consulted" view first; technical details one tap deeper.
- **Trace-anchored feedback.** Thumbs up/down per answer, plus flagging a specific Trace element: _wrong citation · irrelevant chunk · bad machine translation · questionable grade_. Free-text optional. Anonymous (template's anonymous sessions), rate-limited. Feedback lands in the admin review queue; accepted items become **Golden Set** cases. **Implemented (#13):** `POST /v1/feedback` (store-only wiring, guarded by the anonymous Bearer session, metered by the shared /v1 limiter, ADR-0041) takes one feedback shape per request — a thumb (`rating: up|down`) or a flag (`anchor`) — with the category pinned to its element class in the shared contract (a malformed pairing fails the parse). The route resolves the answer's persisted trace through `RagStore.getAnswerFeedbackTarget` (by the trace's `message_id` or a rehydrated chat row id — both uuids) and rejects a flag whose anchor the trace does not ground with 422: chunk ids against the retrieval refs, citation/translation/grade labels against the re-derived citations frame (and a translation/grade flag additionally requires the cited sheet to carry that layer). Rows carry the user id, so the anonymous self-deletion cascade erases them (ADR-0007 amendment). The PWA affordances: anonymous thumbs bar per answer, a flag button per Trace-panel source row, and flag buttons in the citation sheet footer — ids-first externalized copy; anchored flags are recorded with rating −1.
- **Chat history & context awareness** (Postgres-backed sessions).

### 2.2 Quality & safety policy (risk-tiered review)

Hallucination / inaccuracy / deviation from Islamic legal method is the **#1 stated risk**. Controls, in order of cost:

| Control                                 | Coverage                                                             | Mechanism                                                                                                                                                                                                                      |
| --------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Source grounding prompt                 | Always                                                               | Answer only from retrieved context; explicit refusal text                                                                                                                                                                      |
| Citation validator                      | Always                                                               | Deterministic: every citation must exist in retrieved chunks                                                                                                                                                                   |
| Grade flag                              | Always                                                               | Deterministic: dhaif/missing grade → warning                                                                                                                                                                                   |
| Contradiction handler                   | Always                                                               | Conflicting madzhab sources → present both neutrally                                                                                                                                                                           |
| Faithfulness review (LLM, every answer) | ~10% live sample + **all** thumbs-down + full Golden Set pre-release | The reviewer model grades the generator's draft against the retrieved evidence; **same-vendor since the 2026-09-21 amendment** (ADR-0044) — the deterministic citation validator, not vendor separation, carries the invariant |
| Ingestion spot-check (cross-vendor LLM) | 5–10% of cleaned/translated chunks                                   | Different vendor than the cleaner/translator                                                                                                                                                                                   |
| Principle consistency                   | Sampled                                                              | Answer must not contradict retrieved Principles                                                                                                                                                                                |

**Authority order** (per _kaidah ushul_): Quran → Hadith (mutawatir > sahih > hasan; dhaif flagged) → Tafsir → Kitab. Principles are assembled _first in the prompt_ as a lens (presentation order), while the system prompt enforces the Quran-first _authority_ order — the two orderings are deliberately distinct.

### 2.3 UI notes

React PWA from the template (TanStack Router/Query, Tailwind), mobile-first, en/id externalized strings (template guardrail), expandable Trace per message, feedback widget anchored to Trace elements. **Visual language (reworked to the owner-approved reference design):** a single centered column (~max-w-3xl) with a serif-italic KajianQ wordmark over a mono uppercase tagline, an 8-pointed-star logo tile (dark green light / gold dark), a light theme of cream + dark green and a dark theme of near-black + gold (OKLCH tokens mapped into Tailwind 4; `.dark` on `<html>`, persisted in `kajianq.theme`, default light), Cormorant Garamond / Inter / JetBrains Mono / Amiri loaded locally via @fontsource (offline-friendly, PWA-precached), a mono uppercase CONVERSATION rule row, an empty state with serif-italic greeting and three suggestion chips, right-aligned muted user bubbles, assistant answer cards under an avatar header with mono uppercase citation chips, a bottom-pinned rounded composer with a circular submit button, and a mono meta footer — the name stays KajianQ and all copy is id-first externalized. **Implemented (#11, ADR-0040):** the chat is the home route — transcript rehydration on reload (session id + token in localStorage, ADR-0017 anonymous stakes), citation chips resolved from the structured citations frame (passage sheet: Arabic original RTL, translation with the ADR-0006 machine-translation label, grade badge, source), the answer body rendering the model's inline markdown as rich text — `**bold**`/`*emphasis*`/`_emphasis_` spans and `- `/`1.` list blocks, never literal markers (#150; citation chips stay top-level even inside a styled span; the grounded prompt asks for no markdown, so this is display-only resilience), the dhaif warning as a visible card, the ulama disclaimer as a distinct per-answer footer, an honest staged waiting state during generation, and an explicit New-session button. The rehydration transcript is capped at the newest 200 messages and says so — `ChatSessionMessages.truncated` drives an "older messages are not shown" note instead of a silent clip (thermo-review A4). **Recorded gap (thermo-review C2, closed):** the PWA service-worker update prompt (`sw-update.tsx`) was untested everywhere — e2e blocks service workers and no SW unit test existed. The gap is now closed by unit coverage (`apps/web/src/lib/sw-update.test.ts`): the full state machine (no-SW-support guard, first install with no old controller, installing/redundant/installed states, updatefound with no installing worker, apply — SKIP_WAITING postMessage to the waiting worker, then reload on controllerchange, en/id copy) is pinned with mocked `navigator.serviceWorker` surfaces. E2e keeps `serviceWorkers: "block"` deliberately: the update-prompt transition (old controlling worker + byte-different new worker) cannot be staged realistically — Chromium route interception can rewrite the served `sw.js`, but that staging is brittle — and SW mechanics are pinned by unit coverage instead (rate limiting now meters the `/v1` surface only, ADR-0041, so asset loads no longer 429 suites). **Implemented (#12, ADR-0007):** the expandable Trace panel rides every answer as a two-layer disclosure — collapsed by default, the top layer lists the sources consulted in plain language (display titles joined server-side by chunk id; a chunk whose row is missing renders by id, never a fabricated title), and one tap deeper the technical layer shows the router intent, sub-queries, retrieved passages with their fused scores (Intl-formatted), and the model identities — all derived server-side from the persisted answer trace and carried to the PWA in the `ChatTraceFrame` contract (SSE `trace` frame after `citations`; the same frame rehydrates per assistant message). Copy is id-first externalized; the panel is rendered from `MessageCard` because the agentic-limits import cap holds `AnswerCard` at five imports. The trace-anchored feedback widget remains a future ticket. **Implemented (About + Collection pages):** two public static routes join the chat — `/about` (mission, three working principles, privacy posture; ADR-0017 anonymous local-first stakes) and `/collection` (the source register). Both render through the shared `PageShell` (same dashed-rule header, single `max-w-3xl` column) and reuse the existing primitives (`Card`, `MonoLabel`, `PageIntro`, `StatusBadge`). The header gains the primary nav the reference design's header/footer carries — Chat / Collection / About — inline on sm+; below sm it collapses into a burger button whose disclosure menu (`aria-expanded`/`aria-controls`, i18n aria-label, keyboard reachable) closes on route change, matching TanStack `Link` active state (`aria-current="page"`; `/` matched exactly so it cannot read active on a sub-route). The PWA precaches both routes offline: an explicit `navigateFallback: "index.html"` in `vite.config.ts` answers any navigation with the precached shell and the router resolves the path client-side. **The Collection page's content is a typed data module (`apps/web/src/lib/collections.ts` + its data siblings), never the markdown register** — a production bundle cannot read repo files — and it mirrors `NOTICES/DATASETS.md` (attribution lines only; licenses stay in the register), SPECS §4.1/§4.2, and issues #21/#22/#24/#33/#35/#141/#27–#31 (ADR-0014 seed sources, ADR-0026 dhaif-wins grades). Its available/planned distinction is mandatory and visually explicit (filled primary pill vs dashed muted outline), planned entries carry their `planRef` as muted mono (`planned · #33`), and category tabs filter the list. The reference mockup (a Lovable app) supplied structure only — no mockup brand, name, or fictional collection list was copied. **Implemented (#175):** the collection page's "Ask about this source" affordance — each available entry carries a typed TanStack `Link` into the chat with that entry's own per-locale question as the `q` search param (the questions live in the collection data module; planned entries never carry the link, they are registered work rather than a source that can answer). The chat home route (`/`) accepts `?q=<question>`, validated with valibot (an optional string, trimmed, capped at 200 characters; an absent, empty, whitespace-only, non-string or over-long value is IGNORED — never a route-search error), seeds the composer draft from it on read, and CONSUMES the param (a replace navigation, `history.replaceState`) so a refresh cannot re-seed a stale question. The seed is a DRAFT ONLY — never auto-sent — and applies only while the composer is still untouched, so a draft the reader has already edited is never clobbered; the pre-fill never touches the chat session/server contract (ADR-0040) and the composer stays local-only. The affordance label is externalized (`collectionAskAbout`, en/id). **Implemented (#179, ADR-0043, GDPR-C):** the `/about` page's privacy posture section becomes the full Art. 13(1)(e) notice — who processes a visitor's data, how long it is kept, and how to erase it — rendered from a typed register (`apps/web/src/lib/privacy-notice.ts`, component `components/PrivacyNotice.tsx`) rather than hardcoded prose, so ADR-0043 decision 3 (the sub-processor register) and decision 4 (the retention values) stay the source of truth and a register edit is the only way the notice changes. Every row carries a status: the netcup GmbH destination reads **Planned** (the migration is #181, so the notice is never false today) and the rows serving today read **Transition**/**In use today**; cutover is therefore a status edit, not a copy rewrite. A row that denies personal data on a free tier renders **Not for personal data** — the ADR-0009 amendment made visible — and the notice states the register's rule above the table. Retention rows state the ADR's values and whether code enforces them now (the 30-day session TTL in the `RagStore` adapter, the cascade in `DELETE /v1/auth/me`) or a registered ticket will (#180's 14-day access logs and 30-day encrypted backups, #181's snapshot retention); the notice says there is **no separate age-based deletion of chat rows**, so anonymous chat retention is 30 days of inactivity plus on-demand erasure, and it distinguishes clearing browser storage from erasure. The erasure card names the endpoint that exists (`DELETE /v1/auth/me`, cascading the sessions/chat/traces/feedback subtree) and **records the missing in-app control as a gap** — there is no erase button in the web app today, so the copy says so instead of inventing an affordance (also recorded in the Art. 30 record's §10 open items). Copy is id-first externalized (`aboutPrivacy*` keys, en/id); no new personal-data processing is introduced.

---

## 3. Architecture

### 3.1 Monorepo (fork of `agentic-project-template`)

One GitHub repo, Bun workspaces. Mechanical template-sync has been retired
(ADR-0030); every file is project-owned. `packages/` remains the shared-engine
merge path for any future DARS extraction; `apps/` and `package.json` are
project composition. ADRs live in root `adr/` per project convention.

```
apps/
  web/        # React PWA: KajianQ chat UI (+ admin routes, v1)     — from template shell
  api/        # Hono Worker: /v1/chat, /v1/feedback, /v1/admin/*    — from template shell
packages/
  contracts/  # Valibot contracts for all API boundaries            — template pattern
  infra/      # Logger, ConfigStore, ObjectStore,                  — template adapters
              # RagStore (ADR-0008), + Provider adapters
  rag-core/   # DARS: pipeline interfaces — Router, Retriever,      — NEW, domain-agnostic
              # Assembler, Generator, Reviewer
  rag-ingest/ # DARS: source parsers (Tanzil, hadith-json, Shamela),— NEW, domain-agnostic
              # cleaning/translation pipeline, chunking
  eval/       # DARS: benchmark harness, Golden Set runner, judges  — NEW, domain-agnostic
  rate/       # @app/rate: RateLimiter adapter (process-wide    — shared, project-agnostic
              # in-memory, bounded)
  hardening/  # @app/hardening: security headers/CSP + static    — shared, project-agnostic
              # asset serving for the Hono catch-all
  kajianq-domain/ # KajianQ: madzhab enums, principle seed data,    — NEW, the domain pack
              # prompts (ID/EN), citation formatters
scripts/      # Ingestion & eval CLI (Bun, run off-Workers)
```

**Dropped from template:** `packages/local-first` (no offline requirement — chat needs network anyway), D1 + Notes tracer feature. **Reused as-is:** anonymous-session auth (matches anonymous feedback), Valibot contracts, adapters, i18n, Vitest/fast-check/Playwright-BDD, CI gates (size-limit, agentic-limits, truth). The template's Workers deploy pipeline was **not** kept: ADR-0044 replaced it with the VPS deployer (the "topology as code" idea stands; the Cloudflare implementation does not).

**Stage communication:** in-process typed interfaces between packages (modular monolith) — no HTTP between pipeline stages. Per notes.md: simple and maintainable wins; extract a service only when a second consumer actually appears.

**Composition & effect runtime (ADR-0027):** engine packages (`rag-core`, `rag-ingest`, `eval`) and `apps/api` adopt Effect (v4, `effect@4.0.0-rc.112` pinned workspace-wide — the newest rc alchemy@2.0.0-beta.76 supports; see ADR-0027 Appendix C) as the typed effect runtime — seam signatures return `Effect<A, E, R>`, `RunContext` maps to tagged services, `runPipeline` owns a `Scope`, provider fallback uses `Effect.retry` schedules, and SSE deltas are `Stream` with interruption propagation. The persistence seams are Effect-signatured too (ADR-0027 decision 7): `RagStore`/`ObjectStore` return `Effect<A, StoreError, R>` with the closed-kind `StoreError` taxonomy (defined in `@app/rag-core`, re-exported by `@app/infra`), and store resources release via `Scope`. The HTTP edge stays Hono + hono-openapi (`Effect.runPromise` bridge), valibot remains the only schema vocabulary (Effect Schema is not adopted), and the frontend stays plain TypeScript (React Query + valibot) — Effect is never exposed over the API contract. Permanently outside the Effect surface: `packages/contracts`, `packages/rate`, `packages/hardening`, migrations + migration CLI, and `provision/vps/deploy/deploy.sh` (each with a recorded reason and reopen condition in ADR-0027 decision 7). Gate: the Workers-runtime spike (bundle size, cold start, tsgo typecheck) must pass before the migration PRs land.

**Fork guardrail amendment (ADR-0009):** the template's "never add paid services to the critical path" is amended — paid LLM/embedding APIs _are_ the critical path of a RAG product. Price must be weighed in every model decision; cost is traced per query; free tiers are used where quality allows.

### 3.2 Runtime topology (ADR-0008)

**Target topology (ADR-0044, 2026-09-20; the repo-side serving path is live on the box, and the operator's manual is [`docs/VPS-OPERATIONS.md`](docs/VPS-OPERATIONS.md); the executed cutover is [`docs/VPS-CUTOVER-RECORD.md`](docs/VPS-CUTOVER-RECORD.md)):**

```
User → Cloudflare DNS           (DNS-only; the CDN proxy must stay OFF — ADR-0044 d4)
         ▼
       netcup VPS, Germany/EU   (nginx reverse proxy → apps/api as a plain Bun process)
         ├─ apps/web             (static build, served by nginx)
         ├─ Postgres + pgvector  (RagStore adapter over pg/TCP — same schema, self-hosted)
         ├─ systemd: kajianq-api (serving), kajianq-cron (03:17 session reclaim)
         └─ encrypted backups + logrotate (14-day access-log retention, ADR-0043)
       Cloudflare R2            (ObjectStore adapter — raw corpus, snapshot dumps)
External APIs: Gemini (free + paid terms) / Qwen / DeepSeek / TypeSafe  (Provider/Decider seams; ADR-0009, ADR-0042, ADR-0044 amendment)
```

The Cloudflare Workers + Neon path this replaces is **removed, not maintained in
parallel** (ADR-0044 decision 2: there are no live users, so the migration is a
single-shot cutover with no rollback runway; the git history is the rollback).
The Neon row's data carried over in one verified transfer, and decommissioning
the old resources is owner-approved (issue #181).

Ingestion and eval harness run as **Bun CLI scripts** (local/CI), never inside the serving runtime, each pointing at `DATABASE_URL`. Deliberate deviation from the template's D1 — the Smart Router needs SQL metadata filtering + pgvector HNSW + tsvector, which D1/Vectorize cannot express.

**Hosting decision (ADR-0043; serving path implemented under ADR-0044; amended 2026-09-21):** the serving path runs on a **netcup GmbH VPS (Germany/EU)** — EU/Germany data residency for chat content and traces (which can reveal religious convictions, Art. 9 GDPR) at VPS cost, with Postgres self-hosted rather than on Neon's free plan (the adapter speaks `pg` over TCP; ADR-0044 decision 5). Because KajianQ serves the public (SPECS §1.1/§2.1), the Art. 2(2)(c) household exception does not apply and a DPA under Art. 28(3) GDPR is mandatory with netcup GmbH as processor (Art. 28(1)); it is concluded in netcup's Customer Control Panel (Master Data → Order Processing) and the sub-processor register, retention values (30-day anonymous sessions per ADR-0017, 14-day access logs, 30-day rolling encrypted backups, superseded snapshot archives deleted 30 days after their successor verifies), and the snapshot personal-data flag live in ADR-0043. The register rule is the ADR-0009 amendment made enforceable: personal data never routes through a vendor's free tier, enforced per call by `personalDataAllowed` filtering in `provider-factory.ts` for any call that declares `PromptSpec.personalData` — **and ADR-0043's recorded gap is closed**: every serving call site (router, generator, reviewer, embedder) now sets `personalData: true` as a required field on its seam type (a compile error to drop), enforced by `apps/api/src/lib/personal-data-serving.test.ts`, and the serving chains carry paid heads (`gemini-paid` for embeddings, `deepseek` for the router/generator and **the reviewer** — #181). **Reviewer amendment (2026-09-21, ADR-0044 + ADR-0043 amendments):** the reviewer was designed cross-vendor (`kimi:kimi-k2.6` head, ADR-0009/ADR-0015) but requires a Moonshot key the owner does not hold and elected never to provision; of the held keys only DeepSeek has a `generate`-capable paid candidate (TypeSafe is `decide`-only; free-tier Gemini is refused by the enforcement itself). The reviewer therefore runs `deepseek:deepseek-v4-flash`, **same-vendor review accepted deliberately** — the anti-self-review property is relaxed for the current key set, recorded as a trade-off rather than drifted into, with the revisit trigger "a second paid vendor key is provisioned, then re-head the chain (config only)". The `kimi` vendor row and `MOONSHOT_API_KEY` are removed from `models.json`, `env.ts`, `server.ts`'s `PASSTHROUGH_KEYS`, `api.env.example`, and the runbook preconditions. **Deployment is live on the box:** cutover steps 0–3 executed (hardening applied, Postgres restored, snapshot-verified transfer), and the two ADR-0038 evidence labels are `pre-cutover-20260920t1302` (source Neon, verified before transfer) and `post-cutover-20260920t1316` (target VPS pg 17.11, verified through the loopback-only tunnel) — both manifests report 34,389 rows with equal corpus counts and zero ledger drift. The executed log is [`docs/VPS-CUTOVER-RECORD.md`](docs/VPS-CUTOVER-RECORD.md); the operator's as-is manual is [`docs/VPS-OPERATIONS.md`](docs/VPS-OPERATIONS.md). **Controller-side records (GDPR-B #178):** the Art. 30 record of processing — controller, purposes, data categories, retention, TOMs, sub-processor chain, and the CCP declaration block the owner copies — lives in [`docs/GDPR-ARTICLE-30-RECORD.md`](docs/GDPR-ARTICLE-30-RECORD.md), with the Art. 35 screening and Art. 9 mitigations in [`docs/GDPR-DPIA-LITE.md`](docs/GDPR-DPIA-LITE.md). Both derive from ADR-0043 and restate none of its values; concluding the DPA in the netcup CCP remains the owner's action (#178).

**Provisioning & deploys (ADR-0044; supersedes ADR-0028's serving path):** the deployer is its own concern, outside `apps/` — the build → ship → restart → smoke path is [`provision/vps/deploy/deploy.sh`](provision/vps/deploy/deploy.sh), triggered by the reusable [`.github/workflows/deploy-vps.yml`](.github/workflows/deploy-vps.yml) (Staging calls it, so one implementation exists). `apps/*` carries only app code and build scripts; provisioning stays under `provision/vps/`; CI gates stay in `.github/workflows/`. The script builds two Bun-target bundles from `apps/api/src/boot.ts` (serving) and `apps/api/src/cleanup.ts` (the nightly reclamation), so the box needs no `node_modules`; it ships them with `rsync --delete`, restarts `kajianq-api.service`, runs the cron unit once, and smokes the public URL through the proxy and TLS. Local dev and e2e boot the same Bun entry (`playwright.config.ts`'s webServer), so the tested host is the served host. **The deploy identity is a dedicated account (ADR-0044's 2026-09-21 deploy-identity amendment):** `kajianq-deploy`, created by `apply.sh`, owns the deployed tree (so `rsync --delete` needs no group-write grant) and holds exactly two granted commands — `systemctl restart kajianq-api.service` and `systemctl start kajianq-cron.service`, by absolute path and without wildcards — shipped as code in [`provision/vps/sudoers/kajianq-deploy`](provision/vps/sudoers/kajianq-deploy) and installed at `/etc/sudoers.d/` only after `visudo -cf` parses it. The read-only `systemctl is-active` check needs no grant (unit state is world-readable), and the account cannot read the root-only `/etc/kajianq/*.env`. Account, grant, and CI's `VPS_USER` are pinned in agreement by `tests/scripts/vps-hardening.test.mjs`, which also fails on an unused grant or a wildcard; the pre-amendment arrangement (deploy as the owner's admin login on a temporary blanket `NOPASSWD:ALL`, documented as three commands but never installed) is what broke the 2026-09-21 deploy. **VPS privacy hardening is provisioned as code (GDPR-D #180):** [`provision/vps/`](provision/vps/) holds the nginx reverse-proxy server block and its minimal access-log format (client IP and request line, no user-agent/referrer/$remote_user), logrotate stanzas for the proxy (14 days total — current day + 13 rotated segments, size-capped, SIGUSR1 reopen) and Postgres (14 days total, `copytruncate`), a journald cap for API logs (14-day / 512M), the loopback-only Postgres posture, the unprivileged `kajianq-api.service` unit, the nightly `kajianq-cron.service`/`.timer` reclamation (ADR-0017's schedule, re-homed by ADR-0044 decision 7), and the `kajianq-backup.service`/`.timer` daily schedule — placed and enabled by `provision/vps/apply.sh` from root-only `/etc/kajianq/*.env` files (which it refuses to source unless root-owned and 0600-or-tighter). Encrypted-at-rest backups are `provision/vps/backup/kajianq-backup.mjs` (restic client-side encryption, 30-day `forget --keep-daily 30 --prune`, key outside the repo; repository and libpq credentials travel through the environment, never argv); `kajianq-restore.mjs` requires `PGDATABASE_URL`, refuses a target naming the live database by normalized location (host/port/database, not raw string), and re-applies erasure after a restore with the Art. 17 user id bound as a psql variable (drift from production `deleteUserCascade` pinned by test), and `restore-drill.mjs` proves the resurrect-then-erase path end to end (run by the `vps-restore-drill` workflow locally/CI, and by the owner on the host per [`docs/VPS-HARDENING-RUNBOOK.md`](docs/VPS-HARDENING-RUNBOOK.md)). The on-host application and restore drill are **executed** (cutover steps 0–3; the restore drill ran green on the host), and the executed cutover with its evidence and the #181 acceptance-criteria checklist is [`docs/VPS-CUTOVER-RECORD.md`](docs/VPS-CUTOVER-RECORD.md) — the cutover runbook itself is retired, since the migration is done and a fresh box now follows [`docs/SELF-HOSTING-GUIDE.md`](docs/SELF-HOSTING-GUIDE.md) (bare VPS to green deploy, the fork-and-run path). The operator's as-is manual — deploy, Postgres, hardening, monitoring — is [`docs/VPS-OPERATIONS.md`](docs/VPS-OPERATIONS.md).

### 3.3 The DARS pipeline (Smart Router)

1. **Intent & Principle detection** (cheap tier) → JSON: `category, subcategory, madzhab, needs_principle, principle_tags, query_type (factual|ruling|analogy|comparison|history|aqidah), confidence, reasoning`.
2. **Query decomposition** (cheap tier) → 2–4 sub-queries: always one factual; +principle if `needs_principle`; +Quranic dalil if fiqh; +sanad verification if hadith. +Arabic expansion terms from the Terminology Glossary for variant-term queries (ADR-0014; expansion only, never query translation). The router LLM receives the relevant concept-graph slice (1–2 hop subgraph) as prompt context and picks contextually appropriate Arabic terms; expansion candidates are recorded in the Trace.
3. **Source routing** (rules + cheap tier) → index selection + SQL metadata filters (`source_type`, `madzhab`, `grade IN …`, `principle_tags ANY …`).
4. **Retrieval** — hybrid: pgvector HNSW dense + tsvector sparse (upgrade to ParadeDB `pg_search` true BM25 if the server offers it) fused with **RRF (k=60)** + hierarchy bonuses (Quran +0.3, Sahih +0.25, Hasan +0.15, Kitab +0.1, Principle +0.2 on analogy).
5. **Context assembly** — presentation order: Principles → Quran → Hadith → Kitab → concept links; parents contribute LLM summaries so children keep their chapter context.
6. **Generation** (quality tier) — strict grounding system prompt (v1.2 §9.5 rules carried over), streaming.
7. **Post-processing** — citation validator (deterministic), grade flags, disclaimer, ID/EN formatting; sampled faithfulness review enqueued. **Implemented (#8):** the five stages live in `packages/kajianq-domain/src/chat-*.ts` and are wired through the `runPipeline` runner by `runChatPipelinePromise`; `/v1/chat` (`apps/api/src/routes/chat.ts`) streams SSE from the first release (ADR-0034). **Citation payload (#11, ADR-0040):** after the deltas, the route emits one structured `citations` frame derived from the just-persisted answer trace — emitted citations are exactly the answer's inline citation spans that a trace-retrieved chunk grounds (the gate's own grammar; the client never parses answer text for citations) — and `GET /v1/chat/sessions/:id/messages` rehydrates a session's transcript with the same per-message derivation from each persisted trace (display data joined by chunk id via `RagStore.getDocChildrenByIds`; traces read by the `answer_trace_id` FK via `getAnswerTraceById`; the transcript is capped at the newest 200 messages and the response's `truncated` flag makes the cap client-visible — thermo-review A4). **Trace panel payload (#12, ADR-0007):** the route also emits one structured `trace` frame after `citations`, and the rehydration endpoint attaches the same frame per assistant message — the two-layer `ChatTraceFrame` (a plain-language `sources` layer first; a `technical` layer with router intent, sub-queries, chunks with their fused scores and channel ranks, and model identities) derived server-side from the persisted trace, with chunk display titles joined by the trace's own chunk ids. The client renders the frame; it never reconstructs pipeline machinery from raw trace events, and an unresolved chunk row loses its title instead of gaining a fabricated one.

**Implemented (#10) — the answer path is complete and its trust invariant is enforced:**

- **Citations are validated on 100% of answers, deterministically, and a fabricated citation is refused.** The validator (`chat-citation-validator.ts`) scans the answer for the product's citation _grammars_ (`QS.`/`Q.S.`/dot-less `QS` `n:n`, `HR.`/dot-less `HR` `<collection> no. n` — multi-token collection forms like `Sunan Abu Dawud` included, Kitab `Jilid n, Hal. n`) in prose and in brackets, and reports any span that exists in no retrieved chunk. Label normalization canonicalizes the dotted `Q.S.` spelling to `QS.`, so a grounded address written that way is not falsely refused while a fabricated `Q.S. 9:99` is still caught. The reviewer turns a non-empty ungrounded list into the plain refusal and records a `refusal` event with `trigger: "ungrounded_citation"`; the cross-vendor reviewer LLM runs only after that gate passes and can add a second refusal (`verdict: "fail"`), and its `review` event carries the grounded labels plus `verdictParseFailed` when the reply held no readable verdict (an indeterminate review is counted, never silently read as a pass). A citation written with a surah _name_ (`QS. Al-Baqarah:255`) is unverifiable against the corpus's numeric labels and is therefore refused, not waved through.
- **Grounding also excludes interpretation the evidence does not carry.** The generator's strict-grounding prompt answers only from the retrieved context and explicitly forbids adding tafsir, takwil, scholarly opinion, or meaning glosses that the context does not contain — including ones the model knows to be correct. A recognised classical opinion is still an ungrounded claim when the retrieved chunks do not carry it, and the trace cannot show where it came from: in the live smoke (`gs-v0-001`) the generator glossed _"Kursi = (ilmu dan kekuasaan-Nya)"_ from memory, the reviewer failed the draft, and the answer became the plain refusal — the gate working as designed.
- **A demanded-but-absent fact is refused, not explained.** When a question asks for one specific fact (a year, date, number, or name) the context does not state, the generator emits the canonical refusal sentence rather than a grounded explanation that the fact is unknown. The refusal is a **product string**, and refusal detection reads that exact sentence (or a trace `refusal` event) — a well-cited "no one knows, here is the dalil" answer is grounded but is not a refusal, so a question like `gs-v0-019` ("state the exact year") would silently flip between pass and fail on the model's phrasing. Naming the shape in the prompt is what makes the refusal case deterministic; the alternative — a paraphrase-tolerant detector — reintroduces exactly the ambiguity the canonical string exists to remove. **The reviewer gate is the symmetric backstop (2026-09-13, owner-decided after the c7e20a7 Staging failure):** the generator can disobey rule 1 — in that run it produced a grounded, answer-form "no date is stated; only Allah knows" draft that asserted nothing unsupported, so the reviewer passed it, no `refusal` event was recorded, and the refusal-coverage trap failed. The reviewer's Fail ONLY criteria therefore now include **"declines to answer"**: a draft that, for a question demanding one specific fact the evidence lacks, merely describes, explains, or contextualizes what the evidence does or does not say about it FAILS and lands on a refusal (the reviewer-refusal copy, not the generator's canonical insufficiency string). The case is scoped to that decline-to-answer pattern — partial answers and imprecise wording stay passes — and the anti-over-fail guarantees (translation/quoting/citation labels are required behavior; a term the question itself uses is not an unsupported claim) are unchanged, so the gate cannot start inflating refusals on legitimate answers.
- **The reviewer is non-optional on the chat path.** `buildChatWiring` raises a typed config failure when the reviewer role has no keyed candidate, and the route answers 503 — an unreviewed answer is an unverified one, not a lesser one.
- **The reviewer is shown exactly the Generator's evidence.** `buildReviewMessages` passes the assembler's own context turn — the text the Generator answered from — plus the question, so the gate cannot fail an answer for quoting what the prompt provided. Re-rendering the chunks separately had hidden a fallback-track hit's Arabic from the reviewer (its `chunk.text` is the translation layer), which failed a grounded answer as "the Arabic is absent from the evidence" (gs-v0-015); the question rides along so the gate can tell the user's own term from a claim the evidence lacks.
- **Streaming is real but gated.** The generator calls `Provider.stream` (`StreamHandle`) and the route replays the vendor's own delta sequence on the wire; deltas are emitted only after the reviewer validated the _complete_ answer, so an ungrounded citation is never a prefix already delivered to the user. The cost of this choice is time-to-first-token; for a product whose #1 stated risk is hallucinated religious content it is the correct side of the trade. SSE `data:` payloads are emitted one line per line, so an answer containing blank lines (the appended disclaimer, the dhaif warning) survives the frame boundary.
- **Follow-up context.** `POST /v1/chat` loads the session's prior turns (`RagStore.getChatMessages`) _before_ persisting the new question and passes them on `Query.history`; the assembler renders them as a preamble ahead of the evidence. The engine carries history opaquely (ADR-0018 amendment).
- **Product rules are deterministic, not prompt-hoped** (spec §2.2 "Grade flag — Always"): `chat-postprocess.ts` appends the dhaif warning when a retrieved chunk carries grade `dhaif`, the ADR-0006 machine-translation label when the context held a labeled translation the answer dropped, and the not-a-fatwa/consult-ulama disclaimer when the answer omitted it. Never duplicated, never applied to a refusal — the reviewer short-circuits a generator-emitted canonical refusal before any rule runs (round-3 A2): it records a `refusal` trace event (`trigger: "generator_refusal"`), pays no reviewer LLM call, and ships the refusal verbatim. The dhaif-warning suppression anchors to the product-owned warning copy and the technical term `dhaif` (round-3 B4), not to a proximity window around the bare substrings, so an unrelated "lemah" cannot hide the grade. The assembler renders the Arabic original with the labeled translation per chunk — the retriever carries both text layers into the chunk metadata, so the rule is live on the real path, not only on fixtures — and the model never reproduces Arabic from memory.
- **Anonymous sessions (ADR-0017) are mounted:** `POST /v1/auth/anonymous` mints a 30-day Bearer session through `RagStore.createSession`; `DELETE /v1/auth/me` erases the user and their subtree (sessions, chat sessions/messages, feedback, answer traces) via the cascade. Both auth routes wire the **store only** (`buildStoreWiring`) — auth availability is not coupled to any LLM role, so a missing reviewer key cannot take down token minting or the erasure right. A nightly systemd timer (`kajianq-cron.{service,timer}`, `provision/vps/systemd/`, at ADR-0017's 03:17 UTC slot) runs `apps/api/src/cleanup.ts`, which calls `ragStore.cleanupExpiredSessions()` — reclaiming the expired `sessions` rows **and** the anonymous `users` left with no session (the FK cascades user → session only, so users and their chat/trace subtrees would otherwise grow without bound); the entry is inert without a configured store, logs the settled result, and logs rather than throwing on a store fault. ADR-0044 decision 7 re-homed it from the Worker's cron trigger so the schedule stays independently observable and reclaims even when the serving process is unhealthy.
- **Golden Set smoke subset:** `bun run eval:smoke` runs a deterministic, stratified slice (refusal, trap, English, Indonesian coverage first, then stable-order fill — `packages/eval/src/smoke-subset.ts`) through the same harness and ledger as `eval:run`, exiting non-zero on any failure or skip. A live run needs staging secrets (`EVAL_API_BASE_URL`, `EVAL_API_TOKEN`, `DATABASE_URL` — vendor-free since ADR-0044, vendor keys); the selector is unit-tested, the live run is not part of CI.

### 3.4 Providers & default model mix (2026-08, ADR-0009; cost posture amended 2026-09-12)

Generation/embedding calls ride a **Provider interface** (`generate/stream/embed`); decision-model calls (ADR-0042) ride the **`Decider` seam** (`decide`, typed Choice/Score/Noul questions over a state — not chat, so never in a Provider fallback chain). Every stage's model is config-swappable. **Vendor allowlist: Gemini (a free-tier row and a paid-terms row), DeepSeek, Qwen, and TypeSafe AI (decision-model only, bench-gated, ADR-0042) — Moonshot/Kimi was removed 2026-09-21 (ADR-0044 amendment).** Tie-break: near-equal capability → prefer Qwen.

| Role                                                      | Default                                                                                        | Alt / challenger                                                         | Price (in/out per MTok)              |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------ |
| Embedding                                                 | `gemini-embedding-001` (1536-dim MRL) — serving head on the **paid-terms** row                 | Qwen3-Embedding                                                          | free tier (non-serving) / paid $0.20 |
| Cheap tier (router stages 1–3, tagging, cleaning)         | DeepSeek V4-Flash (serving head)                                                               | Gemini 3 Flash Preview (free-tier tail, skipped for personal-data calls) | $0.14/$0.28                          |
| Generator                                                 | **DeepSeek V4-Flash** (cost head, 2026-09-12)                                                  | Qwen3.7-Max (quality head, key not provisioned), Gemini 3.1 Pro          | $0.14/$0.28                          |
| Reviewer (every answer; **same-vendor since 2026-09-21**) | **DeepSeek V4-Flash** (the amendment; was Gemini 3 Flash Preview, then the designed Kimi head) | — (re-head to a second paid vendor when its key is provisioned)          | $0.14/$0.28                          |
| Ingestion translation (AR→ID)                             | Qwen3 Max                                                                                      | Gemini 3 Flash                                                           | $0.78/$3.90                          |

**The generator/reviewer cost posture is owner-decided (ADR-0009 amendment, 2026-09-12; reviewer re-headed 2026-09-21 by the ADR-0044 amendment).** The 2026-09-12 decisions: the DashScope (Qwen) key is absent from this environment and will not be provisioned (recorded in ADR-0036), and #10 made the reviewer mandatory on **every** answer, so §5's "reviewer on a 10% sample" cost line no longer describes the system. The generator therefore runs on DeepSeek V4-Flash as a **single candidate** — the Qwen3.7-Max tail was removed because `resolveRole` silently filters a candidate whose `apiKeyEnv` is unset, so it advertised a failover that could never be wired — and the reviewer gate moved to Gemini 3 Flash Preview, cross-vendor separation preserved (DeepSeek generator, Gemini reviewer). The 2026-09-21 amendment then re-headed the reviewer to `deepseek:deepseek-v4-flash` (same-vendor accepted deliberately; Moonshot removed) because the personal-data enforcement refuses the free-tier Gemini row and the only other held paid key is `decide`-only. A generator failure therefore costs one ≤$1 smoke re-run instead of degrading to a fallback. The trust invariant is carried by the deterministic citation validator plus the reviewer gate, not by generator price — and not by review being cross-vendor: **what the amendment relaxed is vendor separation, not the deterministic gate**, which still refuses a fabricated citation on 100% of answers. The standing duty is the Golden Set run (`bun run eval:run`), which must confirm the chain does not let ungrounded citations through or inflate refusals — a regression there reverts the chain head (one line in `models.json`).

**The embedding default is gate-decided (ADR-0036, 2026-09-10); the serving row is the paid-terms one (#181).** Per ADR-0013 (accepted), retrieval is Arabic-canonical: `text_ar`/`embedding_ar` is the primary evidence index; `text_id`/`embedding_id` is a built-from-the-start fallback/fusion track. The #9 benchmark gate compared `gemini-embedding-001` and `gemini-embedding-2` on ID→AR and AR→AR recall over the real corpus (self-retrieval probes, cosine recall@10) plus the ADR-0014 expansion micro-task; the numbers and the decided retrieval posture are recorded in `adr/0036-embedding-benchmark-gate-retrieval-posture.md` and the run report (`packages/kajianq-domain/fixtures/embed-bench-results.json`). The dual-index schema keeps the posture switchable without re-embedding. Gemini **free-tier** traffic is used for training by Google — acceptable for public religious content and anonymous queries but **never for a serving call**: the retriever embeds the user's sub-queries, which are personal data, so the `embedder` chain head is `gemini-paid:gemini-embedding-001` — the same API, the same model id, the same embedding space on paid terms (a separate vendor row because the register rule keys on terms, not models), so the already-ingested corpus vectors stay valid with no re-embed. The free-tier row survives for non-personal calls (ingestion, tagging). Free-tier embedding quota counts _items_ (≈1,000/day per model) — sized into the gate's stratified-subset corpus.

The harness (notes.md: _"test LLM and give a score for Arabic & Indonesian ability"_) is `packages/eval`: versioned test sets (Arabic comprehension, Indonesian generation, citation discipline, translation fidelity), scoring every candidate; results stored and visible in admin; re-run on model releases.

### 3.5 Data layer

Carried over from v1.2 §5 with changes:

- `doc_parents`, `doc_children`, `principle_index`, `concept_links`, `chat_sessions`, `chat_messages` — unchanged in shape, **except dual embeddings per ADR-0013**: `text_ar`/`embedding_ar VECTOR(1536)` (canonical) + `text_id`/`embedding_id VECTOR(1536)` (fallback/fusion), both built from the start. Sparse search via `to_tsvector('arabic'|'indonesian'|'english', …)` (built-in PostgreSQL configs).
- **New (ADR-0014):** terminology concept graph tables — `concept` (language-neutral concept node, ≈ SKOS Concept / WordNet synset), `lemma` (per-language lemma + `embedding VECTOR(1024)` for BGE-M3 candidate retrieval), `concept_relation` (typed: broader/narrower/related/part_of), `lemma_evidence` (lemma ↔ aligned ayah pairs). Distinct from `concept_links` (passage-level) — the terminology graph is term-level.
- **New:** `aligned_pairs` — one row per aligned (Arabic, Indonesian) ayah pair (`pair_key`, `citation`, `text_primary`, `text_secondary`, `morphology` JSONB), written by Quran ingestion (issue #6); the seed corpus for #24's concept-graph build. Lives in the domain pack's migrations (`kajianq-domain`), per ADR-0014's amendment.
- **New:** `answer_traces` (message_id, router intent JSONB, sub_queries JSONB, chunks JSONB `[{id, rrf_score, rank_dense, rank_sparse}]`, model, tokens_in/out, cost_usd, latency_ms) — powers the user Trace panel and admin. **Implemented (#10):** the persisted shape is the `@app/contracts` `Trace` verbatim (ADR-0007) — `retrieval` events carry the chunk refs with their fused scores, `llm_call` events carry each call's `CostRecord` (model identity, tokens in/out, latency, micro-USD, `estimated`), so the UI's "sources consulted" view and the admin browser read one shape. The chat route writes it for every answer, including refusals and failed runs. The user panel reads it through the server-derived `ChatTraceFrame` (#12) — SSE `trace` frame + rehydration payload — never by shipping raw trace events to the client.
- **New:** `feedback` (message_id, rating, anchor_type `answer|chunk|citation|translation|grade`, anchor_id, category, free_text, status `pending|accepted|rejected`, created_at). **Implemented (#13):** written only through `POST /v1/feedback` — a thumb anchors `answer` (rating ±1, no category); an anchored flag persists rating −1 with the anchor_type/category pairing the shared contract enforces; `user_id` cascade-deletes with the anonymous user, and the row always lands against a persisted, owned trace (malformed anchors are rejected 422, never stored; a degraded server-side derivation — answer text reclaimed, chunk lookup failed — is 503 `anchor_unavailable`, never a user-blaming 422). One verdict per (user, answer, element): the 0003 functional unique index + the store's upsert keeps repeats to a single row (latest wins), and the row is keyed by the trace's canonical `message_id` whichever id the client addressed the answer by (thermo-review A1–A3).
- **New:** `golden_questions`, `eval_runs`, `eval_results` — Golden Set + harness results. **Implemented (#8):** the eval ledger read/write methods (`insertEvalRun`, `insertEvalResult`, `getEvalRun`, `listEvalRuns`, `getEvalResultsByRun`) are on the `RagStore` seam (`rag-store-neon-eval.ts`); per-question outcomes carry the @app/contracts `EvalResultOutcome` JSONB verbatim, and `question_id` stays a loose reference to the product's `golden_questions` table.
- **New:** `model_configs` — registry of provider/model/role assignments (config files remain source of truth; table mirrors for admin display).

### 3.6 Admin (v1 scope)

Routes inside `apps/web` behind a single env-configured admin credential:

1. **Trace browser** — every message's full trace (tokens, cost, raw JSON beyond the user view).
2. **Feedback queue** — accept/reject; accepted → Golden Set.
3. **Harness results viewer** — benchmark/eval runs, trends, model comparisons (harness itself is CLI).

Prompt/model config stays in files, reviewed via PRs — no UI editing in v1.

### 3.7 Integration testing (real, per notes.md)

- **Golden Set**: ~50–100 ID/EN questions with expected source types, required citations, known traps (dhaif hadith, cross-madzhab differences, untranslated kitab, refusal cases). **Implemented (#8):** `golden-set-v0` — 20 questions (17 ID, 2 refusal, 1 dhaif trap), Quran+hadith scope only until kitab ingestion lands; fixture at `packages/kajianq-domain/fixtures/golden-set-v0.json`, `status: "v0-draft"` until the owner signs the content off (the #8 curation gate is `model:plus-human`). A `requiredCitations` label must be reachable in the corpus the gate runs against: re-curating one because retrieval provably cannot surface it is a recorded curation change (fixture `$comment` + the linked recall issue), never a silent re-label. The first such change moved `gs-v0-015` from `QS. 1:1` (dense rank 929/8,305 for the best live sub-query — see #142) to `QS. 1:2`.
- **Metrics**: retrieval recall, citation validity (deterministic), faithfulness (LLM judge). **Implemented (#8):** recall is scored from the served pipeline's persisted answer trace (`retrieval` events), citation validity and refusal correctness are deterministic; the judge role is wired as the pipeline reviewer (**same-vendor since 2026-09-21 — the judge's vendor separation is relaxed, §3.4**), and a standalone faithfulness judge over golden answers remains a later step. See ADR-0034. Citation validity reads its evidence in a fixed order — the SSE `citations` frame (ADR-0040) when the transport carries one, else the trace `review` event's `grounded` labels, else the answer text through the citation gate's own grammar (`citationCandidatesIn` + `normalizeCitationLabel`, injected by the CLI composition root so the engine package stays domain-agnostic) — so a label in a non-empty frame is grounded by construction and a fabricated citation still cannot pass, and the text path no longer scores a grounded marker-spelling variant (`Q.S.`/`QS ` for the fixture's `QS.`) as absent. This is necessary but **not sufficient** for `gs-v0-015`: the observed staging failure (`eval_runs` run `e0de2bf8-5ad6-4618-a0ae-4a96343aabac`, 2026-09-19) retrieved zero Al-Fatihah chunks, so the frame itself was empty and the answer cited only its hadith evidence — a retrieval gap, not a scorer defect, owned by #142 ("short formulaic verses unreachable for whole-surah questions").
- **Cadence**: full suite gates every release + nightly; 5–10 query smoke per PR on free tier. Cost-capped; results in admin. Template's unit/property/BDD gates remain unchanged. **Implemented (#8):** `bun run eval:run` (`packages/eval/scripts/eval-run.mjs`) — hard `EVAL_BUDGET_MICRO_USD` cap covering harness + pipeline spend, per-question rows in `eval_results`, aggregate `EvalRunReport` in `eval_runs`. **Implemented (#10):** `bun run eval:smoke` runs the 5-question PR-time subset (`EVAL_SMOKE_SIZE` overrides) selected by `packages/eval/src/smoke-subset.ts` — stratified so a refusal case, a trap case, an English case, and an Indonesian answer case are always present before the budget fills by stable order; it exits non-zero on any failure or skip, so CI can gate on it.
- **Where the smoke runs (2026-09-12; split into its own job 2026-09-14):** in the `Staging` workflow's `post deploy checks` job, which runs after every staging deploy — a code-bearing push to `main` and an operator dispatch — not in the PR gate set. Docs-only pushes (markdown anywhere, `docs/`, `adr/`, `NOTICES/`, `.agents/`, `.zcode/`) cannot change the deployed Worker, so they skip the push trigger (`paths-ignore`, skipped only when _every_ changed file matches) — a deploy plus its paid smoke would be pure spend with no new artifact to verify; an operator dispatch always remains available. The workflow's `deploy` job guards the secrets (including the chat pipeline's vendor keys) before deploying; the `post deploy checks` job then mints its own anonymous session (`POST /v1/auth/anonymous`, ADR-0017) and runs `bun run eval:smoke` against the deployed Worker with `EVAL_BUDGET_MICRO_USD` always set ($1), so a staging deploy that breaks grounded answering or the refusal gate cannot go green — and a failed deploy skips the checks instead of scanning or scoring a stale environment. PR-time smoke against _PR code_ is not possible without deploying a preview, so the deploy-then-smoke pairing is the gate; `EVAL_SMOKE_SIZE`/`EVAL_BUDGET_MICRO_USD` are dispatch inputs.
- **Staging corpus (one-off, 2026-09-12):** the smoke scores retrieval recall from the persisted traces, so staging must hold a real corpus. It is loaded **once per environment** by an operator running the ingest CLIs locally against `DATABASE_URL` (`NEON_DATABASE_URL` at the time; renamed vendor-free by ADR-0044) (`db:up:all` → `ingest:quran` → `ingest:hadith`, one collection per pass via `--offset`/`--limit 1`) and verifying the landed `sourceType` counts. The `Staging` workflow carries **no** ingest job (ADR-0037): a money-spending job must never sit in the same workflow graph as the evidence job, because a failed ingest would then skip the smoke — spending money and producing no evidence, which is exactly what happened on four runs on 2026-09-12. The store persists across deploys the way production's does. Note the ingest CLIs are idempotent in _persistence_ (upserts by `sourceKey` / `(parentId, ordinal)`) but **not** in _cost_: a pass re-embeds every row it reads, so re-running a collection that already landed pays for it again — resumption is by measured store state, never by re-dispatching. Before this, staging held only `neon-contract` fixtures (`ct-…` keys, no `sourceType` metadata) and an empty `eval_runs` — no ingestion report had ever been persisted there. **Scoped for the MVP (ADR-0039, 2026-09-12):** staging deliberately carries only the _smallest corpus that satisfies the gate_ — Quran plus **one** hadith collection (Malik, ~1,829 records / ~56 MB, landing the store at ~291 MB, 58% of Neon's free 0.5 GB cap), because the free plan **fails writes** on overage and the gate scores behaviour rather than corpus coverage. The full corpus (~35,573 hadith children, ≈1.35 GB projected) is a **production milestone** on the production store, not a staging requirement. The smoke's evidence is therefore a behavioural gate on a partial corpus, and a PR citing it must say so.

---

## 4. Data Strategy

### 4.1 Sources (unchanged from v1.2 §4, licensing duties noted)

| Corpus                     | Source                                             | Format                                   | Licensing duty                                                     |
| -------------------------- | -------------------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------ |
| Quran (Uthmani + metadata) | Tanzil.net                                         | TXT/XML/JSON                             | Public domain text; attribute                                      |
| Terjemahan ID              | Kemenag via Tanzil                                 | TXT                                      | **Verify terms**; attribute (risk §7)                              |
| Tafsir                     | Tanzil / Quran.com (Ibnu Katsir, Jalalayn, Tabari) | XML/JSON                                 | Attribute; prefer public-domain editions                           |
| Hadith ~40K (v1)           | fawazahmed0/hadith-api (ADR-0026)                  | JSON (ara-_/ind-_ editions via jsDelivr) | Unlicense (public domain); attribute + sunnah.com upstream         |
| Hadith 650K w/ sanad       | Sanadset (Kaggle)                                  | CSV (v2, §9)                             | Check Kaggle terms; attribute                                      |
| Hadith API                 | Sunnah.com                                         | JSON API                                 | Key request no longer blocks #7 (ADR-0026); future enrichment only |
| Kitab                      | Shamela `.bok` / OpenITI TEI                       | MDB / XML                                | Classical texts public domain; attribute Shamela/OpenITI           |

All attributions ship in `NOTICES/DATASETS.md` (MIT repo; notes.md requirement).

### 4.2 Priority kitab (pre-600 H)

> **v1 scope rule:** the kitab corpus is the listed priority titles plus the complete _verified_ corpora of the classical tasawuf lineage (below). Edge editions that are themselves later commentaries are out (matn discipline), and disputed attributions are excluded or labeled — never silently ingested. Persian works (Ayyuha al-Walad, Kimiya-i Sa'adat) are in v1 scope only via the verified Al-Ghazali corpus (#33), not as standalone titles; standalone Persian works are post-v1.

Mudawwanah (Sahnun 240H), Al-Umm (Syafi'i 204H), Syarh Aqidah Thahawiyah (321H), Ihya Ulumuddin (505H), Tarikh Tabari (310H), Al-Kamil (Ibn Athir 630H), Muwatta (179H), Musnad Ahmad (241H), Sunan ad-Darimi (255H), Tahdzib al-Akhlaq (421H); medium: Al-Mabsut (483H, partial), Al-Hidayah (593H). Verify matn dates against OpenITI metadata — many Shamela editions are later commentaries (matn vs sharh discipline per `CONTEXT.md`).

**Complete author corpora (verified)** — issues #33 (Al-Ghazali), #35 (Makki, Qushayri, Jilani). Beyond single priority titles, the corpus targets the complete _verified_ works of the classical tasawuf lineage plus Al-Ghazali — all pre-600 H. For every author: the bibliography is LLM-drafted then owner-verified against standard catalogues, and disputed attributions are excluded or explicitly labeled — never silently ingested.

- **Abu Talib al-Makki (d. 386 H)**: Qut al-Qulub fi Mu'amalat al-Mahbub — the foundational tasawuf manual; his other works are not extant.
- **Abu al-Qasim al-Qushayri (d. 465 H)**: Al-Risalah al-Qushayriyyah, Lata'if al-Isharat (tasawuf tafsir), Sharh Asma' Allah al-Husna, Tarikh al-Sufiyyah, Kitab al-Mi'raj; attributed works (e.g., Al-Fusul fi al-Usul) verified before ingestion.
- **Al-Ghazali (d. 505 H)**: the fiqh trilogy (Al-Wajiz, Al-Wasit, Al-Basit), ushul (Al-Mustasfa, Al-Mankhul), aqidah/kalam (Al-Iqtisad fi al-I'tiqad, Tahafut al-Falasifah, Fada'ih al-Batiniyyah, Faysal al-Tafriqah, Iljam al-'Awam, Al-Qistas al-Mustaqim), tasawuf/akhlaq (Ihya Ulumuddin, Mizan al-'Amal, Al-Arba'in fi Ushul al-Din, Bidayat al-Hidayah, Misykat al-Anwar, Al-Maqsad al-Asna, Jawahir al-Qur'an, Al-Munqidz min al-Dhalal), and the Persian works (Ayyuha al-Walad, Kimiya-i Sa'adat — these validate the FA→ID translation path in the harness). Disputed attributions (e.g., Minhaj al-'Abidin, Al-Madnun) excluded or labeled per verification. Note: the Ihya is rich in dhaif hadith — v1 dhaif flagging and v2 per-chain grades (ADR-0012) apply.
- **Abdul Qadir al-Jilani (d. 561 H, Hanbali)**: Al-Ghunyah li Thalibi Tariq al-Haqq, Futuh al-Ghayb, Al-Fath al-Rabbani, Malfuzat, Jala' al-Khawatir (discourses recorded by his students); popular attributions such as Sirr al-Asrar are disputed → excluded or labeled per verification. Adds Hanbali representation to the corpus.

### 4.3 Principle Index

~10–20 entries, **LLM-drafted then verified by the user against Indonesian-language sources** (the core maxims are well documented in Indonesian ushul-fiqh literature; anchors like QS 2:185/2:286/21:107 and Bukhari 39 are checkable without Arabic literacy). Entry shape per v1.2 §4.5. Corpus auto-tagging stays LLM-based with sampled cross-vendor review.

### 4.4 Ingestion pipelines

- **Quran (implemented, issue #6):** `bun run ingest:quran` — acquire Tanzil Uthmani Arabic + Kemenag Indonesian translation + Quranic Arabic Corpus morphology → raw bytes archived to R2 through the ObjectStore seam → the generic `runIngestion` runner (ADR-0021) owns parse + integrity check (6,236 ayah / 114 surah / morphology coverage; the domain `SourceParser` re-parses from the archived bundle, so the archived bytes are the single source of truth) → surah parents with cheap-tier LLM summaries (parent embedding computed from the summary, not full text) + per-ayah children (dual-track `embedding_ar` primary / `embedding_id` fallback per ADR-0013; lemma+root stored per Arabic token per ADR-0014; children written in batches through the RagStore `insertDocChildren` seam) → aligned (AR, ID) ayah pairs persisted for #24's concept-graph build (domain `quran-pair:N:M` key) → IngestionReport persisted to `eval_runs` through the RagStore `insertEvalRun` seam. Idempotent upserts (parents by `source_key`, children by `(parent_id, ordinal)`, pairs by `pair_key`, reports by run id); the CLI is a thin composition root — env access, provider resolution, and I/O all go through the `@app/infra` seams.
- **Hadith (implemented, issue #7):** `bun run ingest:hadith` — acquire per-collection (ara-_, ind-_) editions of fawazahmed0/hadith-api (Unlicense; ADR-0026) → raw bytes archived to R2 through the ObjectStore seam → the generic `runIngestion` runner (ADR-0021) owns parse + integrity check (edition shape, ara/ind alignment on (book, `arabicnumber`) — unmatched pairs and empty-Arabic rows quarantined in the report (`quarantined` = unmatched + `emptyPrimary`), never force-merged; the source's numeric `hadithnumber`/`arabicnumber` are accepted and normalized) → per-(collection, book/section) parents with cheap-tier LLM summaries (parent embedding from the summary; the summary call's cost is recorded by the runner's collector) + per-hadith children (dual-track per ADR-0013; the consolidated grade — dhaif-wins per ADR-0026 — is structured, filterable child metadata, raw per-grader array preserved; empty-ID entries yield `text_id: null`) → aligned (AR, ID) hadith pairs persisted for #24 (`hadith-pair:{collection}:{no}` key, `morphology: []` until the pre-#24 CAMeL Tools enrichment) → IngestionReport persisted to `eval_runs`. Citation renders `HR. Collection no. N (Grade)` from stored fields. Idempotent upserts; thin composition root over the `@app/infra` seams. LLM principle-tagging remains a later step (v1.2 §6.1–6.2).
- **Kitab:** `.bok`→`.mdb` extraction script (Node `shamela` lib or `mdb-tools`) → clean (cheap tier) → metadata extract → principle tag → **translate to Indonesian (Qwen3 Max, labeled)** → hierarchical chunk (parent=bab + LLM summary; child=200–500-token paragraphs, never cut mid-sentence/sanad) → embed → insert. `text_raw` always preserved; originals archived to R2. (v1.2 §6.3 + ADR-0006)
- **Principle Index:** curated seed → embed → auto concept-links to anchored ayat/hadith/kitab.

---

## 5. Cost Model (corrected)

v1.2's "~$2.95 per 1K queries" was **internally inconsistent** (~80K-token contexts would alone cost >$100/1K queries). Realistic model, assuming assembled context ≈ 8–15K input tokens and ~600 output tokens per answer:

| Component                                                        | Per 1K queries                  | Notes                                                                                                                                                                                                                        |
| ---------------------------------------------------------------- | ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Embedding (query)                                                | ~$0                             | Gemini free tier                                                                                                                                                                                                             |
| Router (3 cheap-tier calls)                                      | ~$0–2                           | Gemini 3 Flash free tier; paid fallback priced                                                                                                                                                                               |
| **Generator — DeepSeek V4-Flash**                                | **$1.5–2.5**                    | cost head since ADR-0009 amendment 2026-09-12; 8K ctx ≈ $1.3, 15K ctx ≈ $2.3                                                                                                                                                 |
| Reviewer — DeepSeek V4-Flash (same-vendor, 2026-09-21 amendment) | $1.5–2.5                        | **every answer** since #10 — the reviewer is no longer a 10% sample, so it is a major line; the amendment moved it from paid Gemini onto the generator's own model, cutting it from the largest line to the generator's rate |
| Generator — Qwen3.7-Max (quality head, not wired)                | $20–45                          | future promotion only: re-add to the chain once DASHSCOPE_API_KEY is bound (removed 2026-09-12)                                                                                                                              |
| Reviewer — a second paid vendor (revisit trigger)                | model-dependent                 | re-heading the reviewer to a cross-vendor paid model restores the anti-self-review property (ADR-0044 amendment)                                                                                                             |
| netcup VPS (Germany/EU)                                          | the VPS contract (flat monthly) | replaces the Cloudflare Workers + Neon free tiers; covers proxy, API, self-hosted Postgres, logs, backups                                                                                                                    |
| **Realistic total**                                              | **~$3–10 / 1K queries**         | reviewer-dominated; generator choice swings 15× — this is what the harness is for                                                                                                                                            |

Mitigations: top-k discipline (8–12 chunks, not 20), prompt caching for the stable system prompt + Principle blocks, context trimming, and per-query cost tracing in `answer_traces`.

**Infra cost after the hosting move (ADR-0043/ADR-0044 — the move is executed).** The variable per-query lines above are unchanged: the generator/reviewer/embedding spend does not depend on where the API runs. What changed is the fixed line — `Neon + Cloudflare $0` became:

| Component                                     | Cost                                                | Notes                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| netcup VPS (Germany/EU)                       | the VPS contract (flat monthly)                     | Replaces the Cloudflare Workers + Neon free tiers; covers reverse proxy, API, self-hosted Postgres, logs, backups                                                                                                                                                                                                                   |
| Cloudflare (DNS; the CDN proxy stays **off**) | $0                                                  | Free tier. The proxy must stay off at cutover (ADR-0044 decision 4): with it on, nginx's `$remote_addr` is an edge IP and per-IP metering collapses silently                                                                                                                                                                        |
| Cloudflare R2 (corpus + snapshots)            | $0                                                  | Free-tier object storage for raw corpus and snapshot dumps; snapshot archives carry personal data (ADR-0043) and need encryption                                                                                                                                                                                                    |
| LLM/embedding vendors                         | unchanged (see the table above)                     | The cost head remains the mandatory reviewer per answer                                                                                                                                                                                                                                                                             |
| Backup target (encrypted, off-box)            | the storage endpoint's cost                         | restic client-side encryption (ADR-0043 decision 4); holds a full-DB dump, so it must not be a free tier (the register rule). **Today the repository is a local filesystem on the same box** — an external off-box target is ticket-tracked, and until then the box's own disk is the only copy (a recorded weakness, not a detail) |
| **Effect on the total**                       | **a flat hosting line replacing the $0 free tiers** | Deliberate: EU/Germany residency is bought with a fixed monthly cost, not a per-query one                                                                                                                                                                                                                                           |

The residency choice removes Neon's free-plan ceiling as a cost/scale constraint (ADR-0039 was shaped by it) and replaces it with disk sizing on the VPS — a capital-style decision, not a per-query one. It also removes two free tiers from the personal-data path (ADR-0043's register rule). The hardening (#180) adds one more fixed line — the encrypted backup target — priced here rather than left implicit, because it now stores a full-database dump.

**One-time ingestion (10 kitab):** translation ~$2.50–3/kitab (Qwen3 Max, incl. output tokens) + cleaning/tagging ~$0.30 (DeepSeek V4-Flash) + embeddings ~$0.10 → **~$30–40 total**. Cheap relative to the cost of dirty data in production.

---

## 6. Risks & Mitigations (updated)

| Risk                                                         | Mitigation                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Hallucinated/invented citations                              | Deterministic citation validator (100% of answers); answer-only-from-context prompt; reviewer gate on every answer; Golden Set gate                                                                                                                                                                                                            |
| Answer conflicts with Islamic legal method                   | Usul-derived authority order in prompt; Principle lens; contradiction handler; scholar-visible Traces invite correction                                                                                                                                                                                                                        |
| Gemini embedding underperforms on ID→AR cross-lingual recall | Gate run completed (ADR-0036): gemini-embedding-001 + gemini-embedding-2 compared on ID→AR and AR→AR recall@10 over the real corpus; dual-index schema (ADR-0013) keeps AR-only vs. ID-fallback fusion switchable without re-embedding; Terminology Glossary (ADR-0014) bridges ID→AR explicitly, reducing reliance on cross-lingual embedding |
| Machine translation of classical Arabic is wrong             | Label + Arabic always shown (ADR-0006); sampled cross-vendor review; translation-fidelity suite in harness; `text_raw` preserved                                                                                                                                                                                                               |
| Shamela OCR noise / wrong-author editions                    | LLM cleaning pipeline; matn/sharh discipline; OpenITI cross-check; 100-chunk manual sampling via translator                                                                                                                                                                                                                                    |
| Kemenag translation licensing                                | Verify terms before launch; attribute in NOTICES; fallback to alternate ID translation                                                                                                                                                                                                                                                         |
| Vendor constraint limits quality ceiling                     | Harness tracks allowlist candidates per release; allowlist is policy (ADR-0009), revisitable by the owner                                                                                                                                                                                                                                      |
| Free-tier data usage (Google trains on it)                   | Public content only; no personal data through free tiers; paid tier for sensitive flows                                                                                                                                                                                                                                                        |
| Cost overrun at scale                                        | §5 mitigations; budget alerts; generator downgrade path proven by harness                                                                                                                                                                                                                                                                      |
| Principle Index gaps                                         | Start 10–20, expand iteratively from accepted feedback                                                                                                                                                                                                                                                                                         |

## 7. Plan (12 weeks, approved)

> **Monitoring:** success factors and per-phase metrics live in `docs/SUCCESS_FACTORS_AND_METRICS.md` — the instrument panel for the phases below.

| Phase                              | Weeks | Deliverable (exit criteria)                                                                                                                                                                                |
| ---------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **0. Fork & foundation**           | 1     | Template forked; Notes/local-first/D1 removed; Neon + RagStore adapter live; 4 Provider impls behind interface; fork AGENTS.md amended (ADR-0009); NOTICES/DATASETS.md created; CONTEXT.md + ADRs imported |
| **1. Data & benchmarks**           | 2–3   | Quran + hadith ingested & embedded; hybrid retrieval endpoint with metadata filters; **harness v1 passes: embedding recall + generator citation discipline validated on Arabic/ID test set**               |
| **2. Chat MVP + transparency**     | 4–5   | `/v1/chat` with citation validator; chat UI (ID/EN, tight cards); **user-facing Trace panel**; anonymous sessions; history                                                                                 |
| **3. Router + Principles + admin** | 6–7   | Full Smart Router; Principle Index (~15 curated); trace-anchored feedback; admin (traces, queue, harness results) → **PUBLIC BETA**                                                                        |
| **4. Kitab ingestion**             | 8–10  | Shamela extraction script; cleaning/translation pipeline; 10 priority kitab live with page/bab citations; concept links — informed by beta feedback                                                        |
| **5. Hardening & v1.0**            | 11–12 | Golden Set release gate green; safety layers complete; caching/cost tuning; load test; docs; **v1.0 launch**                                                                                               |

**Operational open tasks:** create vendor accounts (Google AI Studio — free and paid-terms projects, Alibaba Cloud, DeepSeek); request Sunnah.com API key; verify Kemenag translation terms; confirm the self-hosted server's extension support (`pgvector` installed by the engine migration; `pg_search` if available). **Closed/removed by the 2026-09-21 amendments:** Moonshot was never provisioned and is removed from the stack and the register (ADR-0044/ADR-0043 amendments), and the Neon plan-extension question is moot — Neon is decommissioned post-cutover (ADR-0043 decision 1). **Open post-cutover ops work:** an external, off-box backup target (the repository is currently a local filesystem on the same box), and the monitoring build-out in issue #194.

**Phase 3 GDPR workstream (ADR-0043, implemented by ADR-0044):** the hosting move, the DPA/Art. 30 records, the privacy notice, the hardening TOMs, and this migration were a prerequisite set for **public beta**, not a follow-up. The repo-side work is complete and CI-green, and the on-host half is executed through cutover step 3 (hardening applied, Postgres restored, the transfer snapshot-verified and both labels cited); the remainder — the on-host restore drill, the prod cutover, and the owner-approved decommissioning of Cloudflare + Neon — is **executed** (2026-09-21, owner-approved): prod deploy through the `prod` environment's required-reviewer approval gate, posture flipped to production, live flows verified on the box, Cloudflare Workers + Neon deleted (the R2 provenance archive retained), the Worker-era secrets purged, and passwordless sudo removed; the executed log is [`docs/VPS-CUTOVER-RECORD.md`](docs/VPS-CUTOVER-RECORD.md) (2026-09-21 entry). The Staging Golden Set smoke is the post-cutover health signal for phase 3.

## 8. Record of Decisions

| ADR        | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `adr/0005` | Monorepo: DARS engine packages + KajianQ product app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `adr/0006` | Kitab LLM-translated at ingestion, labeled, Arabic always shown                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `adr/0007` | User-facing Trace + trace-anchored feedback (amended: typed `Trace`/`TraceEvent`/`CostRecord` contract in `packages/contracts`; trace owned by the user, erased with them on self-deletion; #13 — the feedback API + widget ship with contract-enforced category↔anchor pairing, server-side anchor validation against the persisted trace, and user-scoped rows in the deletion cascade)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `adr/0008` | Neon Postgres+pgvector behind RagStore adapter (not D1/Vectorize)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `adr/0009` | Vendor allowlist (Gemini/Kimi/DeepSeek/Qwen); paid critical path accepted with price discipline; Qwen tie-break. **Amended 2026-09-12**: generator cost head → DeepSeek V4-Flash as a single candidate (the Qwen3.7-Max tail was removed the same day — DashScope is not provisioned, and a candidate whose key is unset is silently filtered), reviewer gate → Gemini 3 Flash Preview; cross-vendor separation preserved (DeepSeek generator / Gemini reviewer); §5 cost model corrected because #10 made the reviewer mandatory on every answer; the Golden Set run is the standing validation duty and a quality regression reverts the chain heads. **Amended 2026-09-21 (ADR-0044's amendment is the operative record)**: Kimi/Moonshot is removed from the allowlist and every register surface, and the reviewer moves onto the DeepSeek generator model — **same-vendor review accepted deliberately**, with the revisit trigger that a second paid vendor key restores cross-vendor review by config alone. This row's allowlist text is history; the live allowlist is Gemini (free + paid rows) / DeepSeek / Qwen / TypeSafe.          |
| `adr/0010` | Terminology Glossary + Arabic query expansion (never query translation) — **superseded by ADR-0014**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `adr/0011` | Deep Think: iterative budget-capped retrieval mode, never read-all                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `adr/0012` | Per-chain hadith grades via Sanadset isnad data (v2); additive migration, no graph DB / GraphRAG                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `adr/0013` | Arabic as canonical evidence, Indonesian as display; cross-lingual ID→AR retrieval; dual-index schema; #9 as go/no-go gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `adr/0014` | Bilingual terminology concept graph (supersedes ADR-0010's flat table); LLM extraction + human review; prompt-injection consumption (amended: terminology tables relocated to `kajianq-domain`, product tables to `apps/api` — engine schema stays domain-agnostic)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `adr/0015` | No novel legal reasoning: surface classical ta'lil/qiyas as cited, never synthesize new rulings; refusal beats confident gap-filling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `adr/0016` | No corpus-wide GraphRAG: knowledge ships as bounded curated concept structures (#24, #29, principle index, curated concept_links); revisit gate stated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `adr/0017` | Anonymous sessions as first-class KajianQ tables over hosted identity (no Neon Auth in v1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `adr/0018` | AssembledContext carries structured turns + the routed query; Generator owns the final prompt (amended by ADR-0021: stage methods take `RunContext`, Generator/Reviewer return `Draft`; **amended 2026-09-11 by #10**: `Query` gains an optional `history` of prior turns, carried opaquely by the engine and rendered by the domain pack's Assembler — follow-up context)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `adr/0019` | Boundary gate scans engine migration SQL, not just TypeScript (closes the `.sql` blind spot in the domain/vendor/DB-client rules)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `adr/0020` | Neon plan sizing for the dual 1536-dim vector schema (storage/cost trade-off recorded before the #9 gate)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `adr/0021` | `runPipeline` runner owns run scope, run config, and trace assembly; typed dispatch, per-run disposal; cordis deferred behind a revisit trigger                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `adr/0022` | Provider seam in `rag-core`, generic config-driven vendor adapters in `infra` (zero vendor names in engine `.ts`; vendor data lives in checked-in JSON); fallback chains; streaming with deferred cost                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `adr/0023` | Role-agent models resolve from `.zcode/agents/` pins on every harness — DSH reads the pin at dispatch (workflow mapping); a pin that fails to resolve is fixed in the DSH provider config (declare the model id), never rerouted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `adr/0024` | Fork template-sync ownership (historical): `template-sync.json` once listed the byte-identical shared baseline; superseded by ADR-0030                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `adr/0025` | Role-separated GitHub identities: a PreToolUse deny hook redirects role subagents' bare `gh` calls to a `gh-as <role>` wrapper (per-invocation `GH_TOKEN`, token files outside the repo, opt-in `enabled` flag, fail-open); `agent_type` joins the hook-envelope contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `adr/0026` | Hadith v1 source: fawazahmed0/hadith-api (Unlicense, Arabic+Indonesian+grades, 7 collections) replaces hadith-json (unlicensed, ungraded); conservative dhaif-wins grade consolidation, `mutawatir` never self-asserted; Ahmad/Darimi gap accepted; sanad stays in `text_raw` (v2 per ADR-0012); CAMeL Tools morphology deferred to pre-#24. Amended 2026-09-05: weak-class vocabulary enumerated (Mawdu/Batil/Mursal weak, Marfoo not, Mauquf/Maqtu attribution-scope); empty-Arabic rows quarantined (`emptyPrimary`), not run-aborting                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `adr/0027` | Effect (v3) adopted in engine packages + `apps/api` only — typed error channels, `Schedule` retry, `Scope` lifecycle, `Stream` interruption; amends ADR-0021's revisit trigger; Workers spike as go/no-go gate; valibot + Hono edge + plain-TS frontend unchanged; Effect Schema/`@effect/rpc` not adopted; §2 spike gate passed 2026-09-05 (Appendix A: engine-packages-only dep, api bridges via re-exports); amended 2026-09-07 decision 7 (Appendix B): final surface map — RagStore/ObjectStore seams Effect-signatured with `StoreError` taxonomy in rag-core, contracts/rate/hardening/migrations/alchemy.run.ts permanently excluded with reopen conditions, secret-gated `neon:contract` CI job; amended 2026-09-13 (Appendix C): workspace aligned on one v4 version (`effect@4.0.0-rc.112`, the newest rc alchemy supports — rc.113's `Config.string` rename and `mime` drop break `alchemy dev`/deploy, 179 old-API sites (54 `Config.string`), no upstream fix yet) — the dual-version coexistence and the cross-major `runStoreEffect` erasure are retired                                                                          |
| `adr/0028` | Alchemy (v2, Effect-native IaC) owned the whole Worker lifecycle via `apps/api/alchemy.run.ts` — deploys (pinned physical names, one-time `--adopt` bootstrap, `Cloudflare.state()` store) and local dev/e2e (`alchemy dev`: workerd + virtual resources, wrangler retired); Neon stays external; deploy.yml's dead D1 steps removed. **Serving path superseded by ADR-0044 (2026-09-20)**: the Alchemy topology, the Worker entry, the Durable Object rate limiter, and `alchemy dev` as the local/e2e runtime are removed; the deploy path is `provision/vps/deploy/deploy.sh` + `deploy-vps.yml`. The “topology as code, wrangler retired” idea stands                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `adr/0029` | Vite+ (`vp`) owns the check layer — `vp check` (Oxlint + Oxfmt) as the blocking `lint` gate, one-time Oxfmt baseline; API stays alchemy/bun (ADR-0027/0028); amended 2026-09-06: web dev/build/preview and root test run via `vp` (vitest suite verified under Node) — bun stays package manager, bun+alchemy stays API runtime/deploy; amended 2026-09-07: decision 5's version-coupling guard extended to the root `vitest`/`@vitest/coverage-v8` pins vs vp's bundled Vitest                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `adr/0030` | Retire template-sync from KajianQ: delete manifest, state, gate, scheduled workflow, and contracts; all files project-owned                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `adr/0031` | Review feedback owned by a dedicated `fixer` role; fresh context accepts/rejects each thermos item and applies accepted fixes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `adr/0032` | Retire `senior-implementer`; `senior-implementer` writes core code and tests in one run on `model:high` tickets                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `adr/0033` | Reviewer absorbs both thermos passes (security/correctness + maintainability) to fit the three-concurrent-session provider limit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `adr/0034` | Eval harness scores retrieval recall from persisted answer traces (no re-run), one `EVAL_BUDGET_MICRO_USD` accumulator caps harness + pipeline spend with fail-closed abort, and `/v1/chat` streams SSE (`meta`/`delta`/`done`) from day one — the eval client collapses the same stream the PWA consumes (#8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `adr/0035` | The Worker's `DATABASE_URL` binding comes from the `NEON_DATABASE_URL` secret (bind-when-present, ADR-0028 rule; Neon stays external); staging CI requires it (`/v1/chat` is load-bearing there), and the chat OpenAPI documents 400/401/404/502/503 so DAST verifies real behavior — amends ADR-0028                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `adr/0036` | Embedding benchmark gate (#9): `gemini-embedding-001` vs `gemini-embedding-2` on ID→AR/AR→AR recall@10 over the real corpus + ADR-0014 expansion micro-task; winning default set in the `embedder` role; retrieval posture (AR-only vs ID-fallback fusion) decided by the numbers; kitab-scale ingestion unblocked by the gate's acceptance. **Amended 2026-09-11**: expansion micro-task scoring contract tightened post-review (PR #138, C2) to distractor-aware strict scoring — any distractor (trim/lowercase-normalized) picked alongside the expected term fails, self-equal distractor ignored, parseError fails; the checked-in fixture's 12/12 expansion accuracy was measured under the original lenient contract (expected-term-presence only) and is not reproducible under the strict contract (re-score 1/12); the strict contract is the intended posture — no gate re-run forced now (deferral deliberate, not budgetary); the next gate re-run, with the prompt reworded to ask for exactly the expected term, is the first strict-contract measurement; expansion accuracy has no gate floor, so the go decision is unaffected |
| `adr/0037` | Corpus ingest is operator-driven and cost-isolated from the evidence gate: an **ingestion pass** is bounded to an explicit collection range and resumed by measured store state — upserts dedupe rows but not spend, so a landed collection is never re-ingested; and the `Staging` workflow carries **no** corpus-ingest job, so a money-spending job can never gate the smoke evidence job (learned from four 2026-09-12 runs that spent money and then skipped the smoke)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `adr/0038` | The staged corpus is a durable, **paid** asset: every money-spending ingest is bracketed by a verified `pre-ingest` and a `post-ingest` snapshot at two independent layers — a provider (Neon) snapshot plus a portable `pg_dump` + manifest (sha256, byte size, per-table row counts, applied migrations, git sha) written through the ObjectStore seam — and `bun run db:snapshot require <label>` gates the start of any paid run; a snapshot is superseded by a new label, never overwritten. Motivated by measuring the store: 6-hour free-plan PITR, one manual snapshot, and 0 objects in R2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

| `adr/0039` | Staging carries the **smallest corpus that satisfies the gate** — Quran plus one hadith collection (Malik) — because Neon's free plan **fails writes** above 0.5 GB and the smoke scores behaviour (refusal, citation validity, recall by source type), never corpus coverage; the full corpus (≈1.35 GB projected) is a **production milestone** on the production store. Scoping down provably does not weaken the dhaif trap: its target hadith ("seek knowledge unto China") is absent from all seven fawazahmed0 collections, so no collection choice changes what that trap tests. Residual risk accepted: hadith recall against a 22%-share corpus |
| `adr/0040` | Structured citation payload + session rehydration (#11): the server derives a `citations` SSE frame and the rehydration endpoint's per-message payload from the persisted answer trace — emitted citations are exactly the answer's inline citation spans that a trace-retrieved chunk grounds (the gate's own grammar; the client never parses answer text for citations); display data joins by chunk id (`getDocChildrenByIds`), traces read by the `answer_trace_id` FK (`getAnswerTraceById`); chat is the home route with full-transcript rehydration on reload (localStorage session id + token, ADR-0017 stakes); E2E on mocked SSE fixtures, zero LLM spend in CI. **Amended 2026-09-13 (#12)**: the SSE sequence gains one `trace` frame after `citations` and the rehydration payload a per-message `trace` — the two-layer `ChatTraceFrame` derived from the same persisted trace, each frame degrading independently; `technical.models` deliberately discloses the raw config-resolved model ids (ADR-0007 model identity, ADR-0009 opaque ids) |
| `adr/0041` | The per-IP rate limiter meters the `/v1` API surface only — static assets and the `/openapi.json`+`/docs` routes flow through unmetered, while security headers/CORS/correlation-id still apply to every response. Inherited template behavior (assets counted since PR #36, never an ADR decision) stole the per-IP budget from real chat calls behind shared IPs, paid a DO round-trip per asset, and 429'd e2e suites on asset loads that cannot carry a credential. Harness traffic that must exceed the `/v1` budget itself (schemathesis fuzz, load tests) carries a purpose-locked Ed25519 bypass JWT (`X-Rate-Bypass`): public key committed, private key only in `.env`/the `RATE_BYPASS_PRIVATE_KEY` Actions secret, verification fail-closed to ordinary metering, every decision logged; an env-level limiter kill switch is rejected |
| `adr/0042` | TypeSafe AI enters the vendor catalog as the first **decision-model** vendor (ADR-0009 amendment): a new `Decider` seam in `rag-core` (typed Choice/Score/Noul questions over a state, failures ride `ProviderError`) with a vendor-name-free `systemone` adapter in `infra` (cost metered from `usage.input_tokens`, input-only price 42 µ$/MTok, output free). The pinned model `jev-1.13.0` sits in a **bench-only** `decision-candidates` role — no serving role references it. Adoption in any pipeline stage is gated on `bun run eval:decision-bench`: the multilingual gate (ar/id/en × relevance/rerank/citation, 21 verbatim-source cases in `kajianq-domain/fixtures/decision-bench-v0.json`) with floors overall ≥ 0.85 and per-language ≥ 0.75; absent key = NOT RUN exit 0, no passing candidate = non-zero exit. Serving adoption is a future PR that must cite the bench report |
| `adr/0043` | Serving moves to a **netcup GmbH VPS (Germany/EU)** — EU/Germany residency for chat content and traces at VPS cost, with Postgres self-hosted instead of Neon's free plan (ADR-0028's Cloudflare/Alchemy path retained as transition + rollback; the migration is #181). Because KajianQ serves the public, the Art. 2(2)(c) household exception is rejected and a **DPA under Art. 28(3) is mandatory** with netcup as processor (Art. 28(1)), concluded in the CCP (Master Data → Order Processing — owner action, #178). The ADR fixes the **sub-processor register** the notice renders (#179: netcup, Cloudflare/Neon as transition rows, LLM vendors — free-tier vendors may never carry personal data, the ADR-0009 amendment made enforceable via `personalDataAllowed`); the **retention values** (30 days of inactivity for anonymous sessions, ADR-0017 unchanged; 14-day access-log retention; 30-day rolling encrypted backups that re-apply erasure; superseded snapshot archives deleted 30 days after a successor verifies); and an explicit flag that `db:snapshot` **`pre-ingest`/`post-ingest` archives already contain chat/feedback rows** — whole-DB `pg_dump`, so the ObjectStore snapshot prefix is a personal-data-bearing location needing encryption, while `verify`'s corpus-vs-ledger partition (`CORPUS_TABLES`) keeps its exact semantics. Records the **open gap** that no serving call site sets `PromptSpec.personalData` yet, so a chat question can ride free-tier Gemini today — a precondition of public traffic on the VPS, not a nicety. Does not relitigate ADR-0007/ADR-0017. **Implemented by ADR-0044 (2026-09-20, #181)**: the serving path runs as a self-hosted Bun process behind nginx with Postgres over `pg`/TCP; the `PromptSpec.personalData` gap is closed (a required field on every serving seam, paid chain heads); `db:snapshot` is vendor-free (`DATABASE_URL`) and records whether an archive carries personal data in its manifest. **Amended 2026-09-21 (carried with ADR-0044's amendment)**: Moonshot was never provisioned and is removed, so the `kimi` register row is gone and the reviewer is re-headed to `deepseek:deepseek-v4-flash` — **same-vendor review accepted deliberately**, the register rule itself unchanged (DeepSeek stays a paid, `personalDataAllowed: true` row). The on-host application is fully executed (cutover steps 0–7, 2026-09-21: data transfer, serving cutover through the `prod` approval gate, decommissioning of Cloudflare + Neon with the archive verified in two places first, passwordless sudo removed; both snapshot labels cited) |
| `adr/0044` | **The VPS serving path** (supersedes ADR-0028's serving path). A plain **Bun** process behind **nginx** (`apps/api/src/boot.ts` + `provision/vps/systemd/kajianq-api.service`, loopback-bound, SIGTERM-draining) serves the unchanged Hono app; `AppBindings` replaces `WorkerBindings` and drops the host-specific bindings; the rate limiter is the process-wide in-memory backend (one process, so per-process is global) with the client IP digested; Sentry moves to `@sentry/bun`. **Single-shot cutover** with no rollback runway (no live users — a choice against a measured condition, not an omission); the data-integrity criteria stay strict. **The deployer is its own home**: build → ship → restart → smoke in `provision/vps/deploy/deploy.sh` + the reusable `.github/workflows/deploy-vps.yml`; `apps/*` carries no hosting logic. **nginx replaces the bootstrap's Caddy** (:80/:443 have one owner). The `RagStore` adapter speaks **`pg` over TCP** and is renamed for its dialect (`rag-store-postgres*`, provider `postgres`, vendor-free `DATABASE_URL`); the driver adapter reproduces the lazy tagged-template → batched-transaction contract so `createSession` stays atomic. The nightly reclamation becomes a **systemd timer at 03:17** (`provision/vps/systemd/kajianq-cron.timer`); the backup timer is a separate daily unit at **03:15** (`kajianq-backup.timer`); CI's contract suite becomes a `pgvector:pg18` **service container** instead of a shared secret-gated database. **Amended 2026-09-21**: the reviewer chain is re-headed to `deepseek:deepseek-v4-flash` and Moonshot is removed — **same-vendor review accepted deliberately** (the anti-self-review property of ADR-0009/ADR-0015 is relaxed for the current key set), with the revisit trigger "any second paid vendor key being provisioned restores cross-vendor review by re-editing the chain — config only, no code change"; the `kimi` vendor row leaves `models.json` and `MOONSHOT_API_KEY` leaves `env.ts`, `server.ts`, `api.env.example`, and the runbook. **Amended 2026-09-21 (deploy identity)**: a dedicated `kajianq-deploy` account replaces the owner's admin login as the deploy credential, owning the deployed tree and holding exactly two granted systemctl commands shipped as code (`provision/vps/sudoers/kajianq-deploy`, installed behind `visudo -cf`) — the pre-amendment arrangement rode a temporary blanket `NOPASSWD:ALL` rule and broke the first deploy after its removal (Staging run 35548824035); account/grant/`VPS_USER` parity, no-wildcard scoping, and the visudo gate are pinned by `tests/scripts/vps-hardening.test.mjs` |

Domain vocabulary: `CONTEXT.md`. Workflow after this spec: `to-spec` → `to-tickets` per the template's agentic pipeline.

---

## 9. v2 Plan — Sanadset: Isnad data & per-chain grades (targeted for v2.0)

### 9.1 Goal

Close v1's integrity gap. v1 stores one flattened grade per matn, but hadith science grades **chains, not texts**: the same matn can be sahih via one isnad and dhaif via another, and weak chains can strengthen each other (hasan li-ghayrihi). A trust-first product cannot stay confidently wrong in this class of answers. v2 makes grades per-chain with grader attribution and shows transmission evidence (ADR-0012).

### 9.2 Scope

- **In:** Sanadset ingestion (650K hadith, tagged sanad/matn); `narrators` + `hadith_chains` tables; reconciliation against the v1 hadith corpus; per-chain grades in citations, answers, UI, and Trace; narrator-reliability answers; Golden Set v2 gate.
- **Out:** full GraphRAG / any graph DB (ADR-0012 — recursive SQL suffices); narrator biographies beyond Sanadset fields; non-hadith chains (e.g., tafsir isnads) — later.

### 9.3 Data layer (additive migration)

- `narrators`: id, canonical_name_ar, name_variants[], kunya, laqab, death_year, tabaqa, per-critic reliability JSONB, metadata.
- `hadith_chains`: id, child_id → doc_children, collection, narrator_ids[] (ordered), grade, grader, source_ref.
- Teacher/student relations derived from chain adjacency — no separate table in v2.
- Existing v1 rows, embeddings, and indexes untouched — no re-ingestion, no re-indexing.

### 9.4 Ingestion (CLI, off-Workers)

Acquire (Kaggle; **human prerequisite: verify license terms**) → parse sanad/matn tags → narrator entity resolution (deterministic Arabic name normalization → LLM assist on unresolved → sampled cross-vendor review; variants recorded) → matn reconciliation (exact → normalized → fuzzy + LLM adjudication; unmatched ingest as new children; low-confidence quarantined, never force-merged) → attach chains; same-matn variants linked via `concept_links` → ingestion report (match rate, resolution rate, quarantine count, cost).

### 9.5 Retrieval, generation, UI

- Retrieval filters: chain grade, narrator.
- Citation: `HR. Bukhari no. N (Sahih — al-Albani; chain via …)`; multiple chains presented neutrally; dhaif flagged as in v1.
- Prompt rules: never upgrade a grade beyond retrieved evidence; note corroboration when chains support it.
- Trace shows chains consulted; takhrij-style answers when cross-collection evidence exists.

### 9.6 Eval & gate

Golden Set v2 traps (ID/EN): conflicting grades per matn; corroboration cases; narrator reliability; takhrij. Cross-vendor faithfulness judge validates per-chain claims against retrieved chains. v1 suite must stay green. Full gate before v2.0.

### 9.7 Tickets

| Ticket | Deliverable                                       | Blocked by    |
| ------ | ------------------------------------------------- | ------------- |
| #27    | v2 spec (parent)                                  | —             |
| #28    | V2-P1: Schema & domain model                      | #4            |
| #29    | V2-P2: Sanadset ingestion (+ Kaggle terms, human) | #28, #7       |
| #30    | V2-P3: Per-chain grades in retrieval/UI/Trace     | #29, #11, #12 |
| #31    | V2-P4: Golden Set v2 & v2.0 gate                  | #30, #20      |

Sequenced V2-P1 → V2-P4; indicative 4–6 weeks, starts after v1.0 (§7).

### 9.8 Risks

| Risk                                                   | Mitigation                                                                             |
| ------------------------------------------------------ | -------------------------------------------------------------------------------------- |
| Narrator name-resolution errors (Arabic name variants) | Variant table + LLM assist + sampled cross-vendor review                               |
| Wrong matn matches                                     | Conservative thresholds; quarantine bucket; spot-checks                                |
| Kaggle licensing                                       | Verify terms before ingestion (human prerequisite)                                     |
| Scope creep toward GraphRAG                            | Explicitly out of scope per ADR-0012                                                   |
| v1 answers built on flattened grades                   | Semantic upgrade documented (CONTEXT.md Grade); v1 Golden Set re-run gates regressions |

---

_Living document (AGENTS.md §2 rule 16): a PR that changes what this spec describes updates the relevant sections in the same PR. Supersedes `INITIAL_IDEA.md` (v1.2), which is frozen history (its §9 prompt library and §5 schema details remain valid where not amended here)._
